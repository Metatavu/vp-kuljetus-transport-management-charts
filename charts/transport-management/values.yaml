ingress:
  annotations: {}
  labels: {}
  tls:
    secretName: chart-example-tls

tyk:
  hostname: tyk.example.com

auth:
  hostname: auth.example.com

vehicleManagementService:
  hostname: auth.example.com